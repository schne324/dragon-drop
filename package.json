{
  "name": "drag-on-drop",
  "version": "3.0.0",
  "description": "Drag and/or drop",
  "main": "dist/dragon-drop.js",
  "scripts": {
    "build": "npm run pre-build && npm run build:normal && npm run build:min",
    "build:normal": "browserify index.js -o dist/dragon-drop.js --standalone DragonDrop -t [ babelify --presets [ es2015 ] ]",
    "build:min": "browserify -g uglifyify index.js -o dist/dragon-drop.min.js --standalone DragonDrop -t [ babelify --presets [ es2015 ] ]",
    "dev": "watchify index.js -o dist/index.js --verbose --standalone DragonDrop -t [ babelify --presets [ es2015 ] ]",
    "clean": "rm -rf dist/",
    "pre-build": "npm run clean && mkdir dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "foo": "echo yo",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/schne324/dragon-drop.git"
  },
  "author": "Harris Schneiderman",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/schne324/dragon-drop/issues"
  },
  "homepage": "https://github.com/schne324/dragon-drop#readme",
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "babelify": "^7.3.0",
    "browserify": "^14.4.0",
    "component-emitter": "^1.2.1",
    "debug": "^3.0.0",
    "dragula": "^3.7.2",
    "object-assign": "^4.1.1",
    "uglifyify": "^4.0.3",
    "watchify": "^3.9.0"
  },
  "dependencies": {
    "live-region": "^1.0.2"
  }
}
